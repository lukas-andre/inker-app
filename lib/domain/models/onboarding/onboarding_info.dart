class OnboardingInfo {
  final imageAsset;
  final title;
  final description;
  final position;
  final registro;
  final isVisibleSkip;

  OnboardingInfo(this.imageAsset, this.title, this.description, this.position, this.registro, this.isVisibleSkip);
}